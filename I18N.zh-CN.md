# 多语言使用说明

> Seel Widget SDK for iOS
> SDK 内所有文案的翻译方案、接入方式与排错。
> English version: [I18N.md](I18N.md)

## 目录

- [一分钟上手](#一分钟上手)
- [译文从哪来](#译文从哪来)
- [语言怎么定](#语言怎么定)
- [查表规则](#查表规则)
- [占位符与词序](#占位符与词序)
- [文案清单](#文案清单)
- [缓存与刷新](#缓存与刷新)
- [自带文案管线](#自带文案管线)
- [与 Web / RN 的一致性](#与-web--rn-的一致性)
- [排错](#排错)
- [API 速查](#api-速查)

---

## 一分钟上手

```swift
import SeelWidget

SeelWidgetSDK.shared.configure(
    apiKey: "your_api_key",
    environment: .production,
    adminDomain: "your-store.myshopify.com"   // 有它才会去拉多语言配置
)
```

就这一行 `adminDomain`。SDK 会按设备语言拉取商户的文案配置并自动应用,界面上的文字随之切换。

不传也能跑——只是没有译文,全部显示 SDK 内置的英文。

---

## 译文从哪来

SDK 合并两路来源,**报价这一路优先**:

| 来源 | 时机 | 覆盖范围 |
|---|---|---|
| `GET /v1/shopify/merchant-configs` → `merchants[0].i18n_config` | `configure` 时(需要 `adminDomain`) | SDK 全部文案 |
| `POST /v1/ecommerce/quotes` → `extra_info.i18n` | 每次报价 | 该笔报价的专属措辞 |

两路的结构完全一样:

```jsonc
{
  "lang": "fr-FR",
  "texts": [
    { "key": "Privacy Policy", "value": "Politique de confidentialité" },
    { "key": "Secure Your Purchase Now", "value": "Protégez votre achat" }
  ]
}
```

分开存、合并读:换一笔报价不会把商户级的基础译文洗掉。

**任何一路失败都不会导致界面空白**——查不到就回落英文原文。

---

## 语言怎么定

```swift
// 跟随设备语言（默认）
SeelWidgetSDK.shared.language = nil

// 或者指定
SeelWidgetSDK.shared.language = "fr-FR"

// 看看实际用的是哪个
print(SeelWidgetSDK.shared.resolvedLanguage)   // "fr-FR"
```

设备语言会先归一成后端认识的标识,规则与 Shopify 前端脚本的 `LANG_LIST` 一致:

| 设备返回 | 归一后 | 说明 |
|---|---|---|
| `zh-Hans-CN` | `zh-CN` | 去掉 script 子标签 |
| `zh-Hant-TW` | `zh-TW` | 繁体按地区区分 |
| `zh-Hant-HK` | `zh-HK` | |
| `fr-CA` | `fr-FR` | 后端不支持该地区,退到该语言的默认地区 |
| `en-GB` | `en-US` | 同上 |
| `nn-NO` | `nb-NO` | 挪威语归并 |
| 完全不认识 | `en-US` | 兜底 |

改 `language` 会重新拉配置,并让已经在屏幕上的视图重新渲染,不需要重建任何组件、也不会重新发报价请求。

---

## 查表规则

**key 就是英文原文。** 这不是笔误——后端文案库以英文原文作为 key,Shopify 店面脚本和 React Native SDK 都是这个约定,iOS 保持一致,后端配一次三端同时生效。

查表顺序:

```
报价响应的译文  →  merchant-configs 的译文  →  SDK 内置英文
```

服务端随报价下发的文案(`widget_title`、`display_widget_text`、`coverage_details_text`、`widget_disclaimer`)**也走同一张表**。它们本身是英文原文,所以同样能查到译文——这一点和 Web 端的 `i18nConfigTextsJson[text] || text` 完全一致。

---

## 占位符与词序

部分文案带 `{{占位符}}`,由 SDK 在客户端填充:

| 文案 | 占位符 |
|---|---|
| `{{title}} for {{price}}` | 保障名称、价格 |
| `Only {{price}} for Complete Peace of Mind` | 价格 |
| `Powered by {{seel}}` | 品牌名 |

**词序由模板决定,不是拼接出来的。** 例如法语译文写成:

```json
{ "key": "{{title}} for {{price}}", "value": "Pour {{price}}, ajoutez {{title}}" }
```

界面上就会渲染成「Pour **$3.75 USD**, ajoutez **Worry-Free Purchase®**」——价格跑到了前面,而且标题和价格各自保留原有的字号字重。所以译文里**占位符可以任意换位置**,不必迁就英文语序。

没有对应值的占位符会原样保留(如 `{{price}}`),便于在联调时一眼看出漏传。

---

## 文案清单

下表是 SDK 会去查的全部 key。**「RN 对应」列**给出 `react-native-seel-widget` 里 `KeyValue` 的字段名,方便和后端对齐。

### 与 RN SDK 共用

| 英文原文（key） | RN 对应 | 出现位置 |
|---|---|---|
| `{{title}} for {{price}}` | `wfp_title` | widget 标题、详情页标题 |
| `Powered by {{seel}}` | `powered_by` | widget 底部、弹窗底部 |
| `Worry-Free Purchase®` | `product_name` | 标题缺省时的兜底 |
| `We're unable to offer Worry-Free Purchase® for this order. This could be due to one or more of the following reasons` | `ineligible_main_message` | 拒保提示开头 |
| `Shipping destination not supported` | `ineligible_reason_shipping` | 拒保原因 |
| `Checkout currency not accepted` | `ineligible_reason_currency` | 拒保原因 |
| `Order value exceeds our coverage limit` | `ineligible_reason_value` | 拒保原因 |
| `Item(s) not eligible for this service` | `ineligible_reason_items` | 拒保原因 |
| `Our system has flagged this order as ineligible` | `ineligible_reason_system` | 拒保原因 |
| `If you have any questions, please contact our customer support team for assistance` | `ineligible_support_message` | 拒保提示结尾 |
| `We've Got You Covered` | `coverage_title` | 弹窗头图标题 |
| `Only {{price}} for Complete Peace of Mind` | `pricing_message` | 弹窗头图副标题 |
| `What's Covered` | `whats_covered_title` | 保障范围卡片 |
| `Get a Full Refund, No Questions Asked` | `get_full_refund` | 默认版式保障块 |
| `Privacy Policy` | `privacy_policy` | 底部链接 |
| `Terms of Service` | `terms_of_service` | 底部链接 |
| `Secure Your Purchase Now` | `cta_secure_purchase` | 主按钮 |
| `Continue Without Protection` | `cta_continue_without` | 次按钮 |
| `Easy Resolution` | `easy_resolution` | 默认版式 |
| `Resolve your issues with just a few clicks` | `resolve_with_clicks` | 默认版式 |
| `Complete Peace of Mind` | `complete_peace_of_mind` | 默认版式 |
| `Zero-risk on your order with our protection` | `zero_risk` | 默认版式 |
| `Get your refund promptly` | `get_refund_promptly` | 默认版式 |

### 取自 Shopify 店面脚本的 `en.json`

| 英文原文（key） | 出现位置 |
|---|---|
| `Instant Resolution` | 弹窗功能卡 |
| `Quick resolution in just a few clicks` | 弹窗功能卡 |
| `Get help anytime with fast response` | 弹窗功能卡 |

### iOS 独有

这些 key 在 RN 的 `KeyValue` 和 Shopify 的 `en.json` 里都没有。**后端文案库如果没收录,它们会一直显示英文**,需要翻译得先让后端补上。

| 英文原文（key） | 出现位置 |
|---|---|
| `Opt-In Now for Full Protection` | 默认版式主按钮 |
| `No Need` | 默认版式次按钮 |
| `Close` | 弹窗关闭 |
| `What's Covered by Seel` | 默认版式小标题 |
| `24/7 Support by Seel` | 弹窗功能卡 |
| `Worry-Free Purchase® available with` | 商详 banner |
| `Loading...` | 内置 WebView 标题 |
| `Selected` / `Unselected` | 勾选框无障碍朗读 |
| `On` / `Off` | 开关状态朗读 |
| `Double-tap to add or remove this protection` | 无障碍操作提示 |
| `About this protection` | info 按钮朗读名 |
| `Back` | WebView 返回按钮朗读名 |
| `Double-tap to learn why this protection is unavailable` | 拒保态无障碍提示 |

> 最后 7 条只供 VoiceOver 朗读,不出现在视觉界面上,但同样建议翻译——否则读屏用户会在已翻译的界面里听到英文提示。

---

## 缓存与刷新

- 译文**按语言缓存在 `UserDefaults`**。冷启动时接口还没回来,界面先用上次的译文渲染,不会闪一下英文。
- 拿到新响应后覆盖缓存。
- 切换语言会清掉当前译文并重新拉取。
- 译文变化时 SDK 内部发出 `SeelI18nDidChange`,widget、banner、正在展示的弹窗都会自动重建文案。

宿主如果有自己的界面需要同步刷新:

```swift
NotificationCenter.default.addObserver(
    forName: .SeelI18nDidChange, object: nil, queue: .main
) { _ in
    // 你自己的刷新逻辑
}
```

---

## 自带文案管线

如果你已经有自己的翻译流程,或者需要离线 / 测试场景,可以直接注入:

```swift
SeelI18nManager.shared.setTexts([
    "Privacy Policy": "Politique de confidentialité",
    "Secure Your Purchase Now": "Protégez votre achat",
])
```

注入的内容与 merchant-configs 同层,仍会被报价响应里的译文覆盖。`SeelI18nManager.shared.reset()` 清空,回到内置英文。

---

## 与 Web / RN 的一致性

| | Shopify 店面脚本 | React Native SDK | iOS SDK |
|---|---|---|---|
| 译文来源 | merchant-configs | 报价响应 | **两路都接** |
| 查表 key | 英文原文 | 英文原文 | 英文原文 |
| 兜底 | `dict[text] \|\| text` | `dict[KeyValue.x] ?? '英文'` | 同左 |
| 占位符 | `{{...}}` 客户端替换 | 同左 | 同左 |

三端查的是同一批 key,后端配一次译文全都生效。iOS 之所以两路都接:merchant-configs 进 App 就能拿到、覆盖全部界面;报价那一路能带单笔订单的专属措辞。

---

## 排错

**界面还是英文**
1. 确认传了 `adminDomain`——不传就不会去拉配置。
2. 打开 `.development` 环境看控制台:`merchant-configs loaded => lang: fr-FR, texts: 42`。`texts: 0` 说明后端没给这个语言的文案。
3. 用 `SeelWidgetSDK.shared.resolvedLanguage` 确认归一后的语言标识是不是你以为的那个。

**只有一部分文案翻译了**
后端文案库里缺少那几条英文原文。对照上面的[文案清单](#文案清单)把缺的 key 补给后端——尤其注意「iOS 独有」那一组。

**拒保提示的标点看着奇怪**
文案库里的原文结尾不带 `:` 和 `.`,标点是 SDK 拼上去的。译文里也**不要**带结尾标点,否则会出现两个。

**占位符没被替换,界面上直接显示 `{{price}}`**
说明译文里的占位符名字拼错了。名字必须与英文原文里的完全一致(`{{title}}` / `{{price}}` / `{{seel}}`)。

**改了 language 没反应**
确认调用在主线程。另外 `language` 只接受能被归一的标识,传 `"火星文"` 会退回 `en-US`。

---

## API 速查

```swift
// 配置时给出店铺域名，自动拉取并应用译文
func configure(apiKey: String, environment: SeelEnvironment = .production, adminDomain: String? = nil)

// 手动刷新（或稍后再补域名）
func loadMerchantConfigs(adminDomain: String? = nil,
                         completion: ((Result<MerchantConfigsResponse, NetworkError>) -> Void)? = nil)

// 语言：nil 表示跟随设备
var language: String?
var resolvedLanguage: String
var adminDomain: String?

// 直接注入 / 清空译文
SeelI18nManager.shared.setTexts(_ texts: [String: String])
SeelI18nManager.shared.reset()
SeelI18nManager.shared.isLoaded

// 译文变化通知
Notification.Name.SeelI18nDidChange
```
